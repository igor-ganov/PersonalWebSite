{
  "name": "personalwebsite",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:ts": "tsc -p tsconfig.ts.json",
    "build:scss": "sass src:dist --no-source-map",
    "build": "npm run build:ts && npm run build:scss && npx @11ty/eleventy",
    "start": "npm run build:ts && npm run build:scss && npx @11ty/eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.0"
  },
  "devDependencies": {
    "@types/react-dom": "^19.1.5",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "sass": "^1.77.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
